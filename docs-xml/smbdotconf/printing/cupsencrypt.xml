<samba:parameter name="cups encrypt"
	context="G"
	type="enum"
	advanced="1" print="1"
	xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    このパラメーターは、 <smbconfoption name="printing"/> が
    <constant>cups</constant> に設定され、 CUPS が 1.0.x 以降の場合のみ
    有効となり、 Samba が CUPS サーバーと通信する際に暗号化を行うか
    どうかを指定する。設定可能な値は、
    <emphasis>auto</emphasis> 、 <emphasis>yes</emphasis> 、
    <emphasis>no</emphasis> である。
  </para>

  <para>
    auto に設定した場合、各 CUPS コネクションの確立時に TLS ハンドシェイクを
    試行する。失敗した場合は、暗号化なしの処理に切り替える。
  </para>

</description>

<value type="default">"no"</value>
</samba:parameter>
