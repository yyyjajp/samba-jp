<samba:parameter name="print notify backchannel"
		 context="S"
		 print="1"
		 type="boolean"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
<para>Windows のプリンタークライアントは、そのクライアントへの、バックチャネル
      SMB コネクションを待ち受けることでプリントキューのステータスを更新する
      ことが出来る。クライアントのファイアウォール設定により、これは、重要な
      タイムアウトを発生させる場合があり、クライアントが SMB サーバーとして動作
      していても、その許可がないようにみえるように、しばしば失敗するだろう。
      既定値では、Samba 印刷サーバーはクライアントに対しての接続を試みなくなり、
      対応する要求をクライアントへの接続が失敗したように取り扱う。
      </para>
</description>
<value type="default">no</value>
</samba:parameter>
