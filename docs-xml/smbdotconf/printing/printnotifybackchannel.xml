<samba:parameter name="print notify backchannel"
		 context="S"
		 print="1"
		 type="boolean"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
<para>Windowsのプリンタークライアントは、そのクライアントへの、バックチャネル
      SMBコネクションを待ち受けることでプリントキューのステータスを更新する
      ことが出来る。クライアントのファイアウォール設定により、これは、重要な
      タイムアウトを発生させる場合があり、クライアントがSMBサーバーとして動作
      していても、その許可がないようにみえるように、しばしば失敗するだろう。
      このパラメーターを<constant>no</constant>と設定することで、Samba印刷
      サーバーはクライアントに対しての接続を試みなくなり、対応する要求を
      クライアントへの接続が失敗したように取り扱う。既定値の設定は
      <constant>yes</constant>で、これはsmbdに、この接続を行わせる。
</para>
</description>
<value type="default">yes</value>
</samba:parameter>
