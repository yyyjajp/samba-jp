<samba:parameter name="allow nt4 crypto"
                 context="G"
                 type="boolean"
                 advanced="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    このオプションは、netlogon サーバ(現在 'active directory ドメインコントローラ'
    モードのみ)が、NETLOGON_NEG_STRONG_KEYS も、NETLOGON_NEG_SUPPORTS_AESも
    サポートしないクライアントを拒否するかどうかを制御する。</para>

    <para>
      このオプションは、Samba 4.2.0 で追加された。これは、影響のある既定値が "yes"
      であるので、現在は "no" となっているため、Samba 4.1.xまでの
      バージョンで問題なく動いているクライアントをロックアウトするかもしれない。
    </para>

	<para>This option was added with Samba 4.2.0. It may lock out clients
	which worked fine with Samba versions up to 4.1.x. as the effective default
	was "yes" there, while it is "no" now.</para>

	<para>
	  もしも、レジストリ中に、RequireStrongKey = 1 を設定していないクライアントが
	  あるならば、すべてのクライアントを修正するまで、"allow nt4 crypto = yes"
	  を設定しなければならない。
	</para>

	<para>"allow nt4 crypto = yes" とすることで、おそらくダウングレード攻撃
	経由で、暗号化レベルを下げてネゴシエーションすることが出来る。</para>

	<para>If you have clients without RequireStrongKey = 1 in the registry,
	you may need to set "allow nt4 crypto = yes", until you have fixed all clients.
	</para>

	<para>"allow nt4 crypto = yes" allows weak crypto to be negotiated, maybe via downgrade attacks.</para>

	<para>このオプションは、'reject md5 clients' オプションの前に必要である。</para>
</description>

<value type="default">no</value>
</samba:parameter>
