<samba:parameter name="logon drive"
                 context="G"
				 type="string"
                 advanced="1" developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメーターは、
	ホームディレクトリとして接続されるパスを指定する
	(<smbconfoption name="logon home"/> も参照のこと)。
	このパラメーターは NT Workstation によってのみ利用される。</para>

	<para>このパラメーターは、Sambaがログオンサーバーとして構成された場合のみ、
	意味をもつことに注意。</para>
	<para>訳注: Samba 3.0 系列で LDAP や TDB を認証データベースとして用いている場合、これらの値はユーザー毎に保持され、本パラメーターは指定がなかった際の既定値を設定するものとなる。</para>
</description>
<value type="default"></value>
<value type="example">h:</value>
</samba:parameter>
