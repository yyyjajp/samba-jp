<samba:parameter name="add machine script"
                 context="G"
				 type="string"
                 advanced="1" developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメータは、管理者のユーザ名とパスワードを指定する方式によってマシンがドメインに追加された際に、 <citerefentry><refentrytitle>smbd</refentrytitle>
	<manvolnum>8</manvolnum></citerefentry> によって実行されるスクリプトのフルパス名である。</para>

	<para>このオプションは、 smbpasswd のように SAM バックエンドの RID の計算方式が UNIX の UID に関連づけられている場合にのみ必要とされる。
	このオプションは Samba 3.0 でのみ利用可能である。</para>
</description>

<value type="default"/>
<value type="example">/usr/sbin/adduser -n -g machines -c Machine -d /var/lib/nobody -s /bin/false %u</value>
</samba:parameter>
