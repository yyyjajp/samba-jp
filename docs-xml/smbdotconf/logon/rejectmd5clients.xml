<samba:parameter name="reject md5 clients"
                 context="G"
                 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>このオプションは、netlogon サーバ(現在 'active directory
  ドメインコントローラ' モード中のみ)が、NETLOGON_NEG_SUPPORTS_AES
   をサポートしないクライアントを拒否するかどうかを制御する。</para>

   <para>すべとのドメインメンバが aes をサポートする場合に、
   これを yes に設定できる。そうすると、ダウングレード攻撃を防止できる。</para>

	<para>このオプションは、 'allow nt4 crypto' オプション に優先する。</para>
</description>

<value type="default">no</value>
</samba:parameter>
