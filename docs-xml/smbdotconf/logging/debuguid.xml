<samba:parameter name="debug uid"
                 context="G"
				 type="boolean"
                 developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>Samba は root 権限で動作する時もあれば、
    接続したユーザーの権限で動作する時もある。
    このパラメーターを有効にすると、現在の euid、egid、uid、gid をログファイル中のタイムスタンプメッセージに付加する。</para>
		
	<para>
	このパラメーターを有効にする際は、 <smbconfoption name="debug
	timestamp"/> が有効になっている必要があることに注意。</para>
</description>
<value type="default">no</value>
</samba:parameter>
