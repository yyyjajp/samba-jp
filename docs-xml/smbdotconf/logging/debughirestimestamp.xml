<samba:parameter name="debug hires timestamp"
                 context="G"
				 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>ログメッセージに秒単位以上のタイムスタンプが必要な場合、
	この真偽値のパラメーターを有効にすることにより、
	マイクロ秒単位のタイムスタンプがメッセージに付加される。
	</para>

	<para>
	このパラメーターを有効にする際は、 <smbconfoption name="debug
	timestamp"/> が有効になっている必要があることに注意。</para>
</description>
<value type="default">yes</value>
</samba:parameter>
