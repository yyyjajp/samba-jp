<samba:parameter name="syslog only"
                 context="G"
				 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>このパラメーターが設定されると、 Samba のデバッグメッセージが
	システムの syslog にのみ記録されるようになり、
	デバッグログファイルに出力されなくなる。
      もしも、<emphasis>syslog only</emphasis>が有効になっていたとしても、
      log.[sn]mbdへのロギングは引き続き行われる。
    </para>

    
        <para><smbconfoption name="logging"/>を代わりに使うべきである。
	<smbconfoption name="logging"/>が設定された場合、それは、
	<smbconfoption name="syslog only"/>パラメータを上書きする。</para>
	
</description>
<value type="default">no</value>
</samba:parameter>
