<samba:parameter name="reject md5 servers"
                 context="G"
                 type="boolean"
                 advanced="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>このオプションは、winbind が、netlogon セキュアチャネルでの
  AES サポートをのサポートを必要とするかを制御する。</para>

  <para>以下のフラグは、NETLOGON_NEG_ARCFOUR、
  NETLOGON_NEG_SUPPORTS_AES、NETLOGON_NEG_PASSWORD_SET2 と
  NETLOGON_NEG_AUTHENTICATED_RPC を必要とする。</para>

  <para>もしもすべてのドメインコントローラが AES をサポートしているなら、
  これを yes にすることができる。</para>

  <para>動作は、'reject md5 servers:NETBIOSDOMAIN = yes' をオプションとして
  使う事で、 netbios ドメインごとに制御出来る。</para>

  <para>このオプションは、<smbconfoption name="require strong key"/> を必要とする。</para>
</description>

<value type="default">no</value>
</samba:parameter>
