<samba:parameter name="include system krb5 conf"
                 context="G"
                 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
       <para>
	 この値を<value type="example">no</value>に設定すると、winbind が
	 作成する krb5.conf 中に、システムの /etc/krb5.conf を含めないようにする。
	 <smbconfoption name="create krb5 conf"/>も参照のこと。このオプションは
	 MIT Kerberos を使うように Samba が構築されている場合にのみ使える。
        </para>

</description>
<value type="default">yes</value>
</samba:parameter>
