<samba:parameter name="winbind max domain connections"
		 context="G"
		 type="integer"
		 advanced="1" developer="1"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメータは、<citerefentry><refentrytitle>winbindd</refentrytitle>
	<manvolnum>8</manvolnum></citerefentry>デーモンが、1つのドメインの
	ドメインコントローラに対してオープンすべき最大同時接続数を
	指定する。
	このパラメータの値を1より大きくすると、いくらか遅い場合、
	大量の同時winbind要求に対してスケーラビリティを改善できる。
	</para>
	<para>
	  もしも、<smbconfoption name="winbind offline logon"/>
	  が<constant>Yes</constant>の場合、この設定をしても、
	  ドメイン単位に1つのDC接続のみが許可されることに注意。
	</para>
</description>

<value type="default">1</value>
<value type="example">10</value>
</samba:parameter>
