<samba:parameter name="winbindd socket directory"
                 context="G"
                 type="string"
                 constant="1"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>この設定は、winbindのソケットの位置を制御する。</para>
  <para>自動テストスクリプトをのぞき、クライアントツール(nss_winbind など)が、
  このパラメータを受け取らないため、これを変更すべきではない。クライアントツールは
  WINBINDD_SOCKET_DIR 環境変数で、パスを変更するようにしなければならない。</para>
</description>

<related>winbindd privileged socket directory</related>

<value type="default">&pathconfig.WINBINDD_SOCKET_DIR;</value>
</samba:parameter>
