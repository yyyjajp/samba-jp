<samba:parameter name="winbind sealed pipes"
                 context="G"
                 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このオプションは、winbind からドメインコントローラに  pipe を使う任意の要求が
	保護されるかを制御する。保護を無効にすると、デバッグには便利である。</para>

	<para>動作は、'winbind sealed pipes:NETBIOSDOMAIN = no' オプションを使うことに
	よって、netbios ドメインごとに制御出来る。</para>
</description>

<value type="default">yes</value>
</samba:parameter>
