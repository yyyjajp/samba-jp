<samba:parameter name="idmap domains"
                 context="G"
		 type="string"
                 advanced="1" developer="1" hide="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>
	idmap domains オプションは、WinbindのSID/uid/gidのテーブルを
	管理するためにそれぞれ別々の設定がなされたバックエンドを持つ
	Windowsドメインのリストを定義する。
	このパラメーターは、以前の <smbconfoption name="idmap backend"/>
	オプションと同時に利用することはできない。
	</para>

	<para>
	値は、Winbindのプライマリドメイン、および信頼関係にあるドメインの
	ショートドメイン名から構成される。リストに指定されていない
	ドメインのための包括的なドメイン名として、任意の文字列のドメインを
	指定することも可能である。
	</para>

	<para>
	それぞれのドメインの SID/uid/gid バックエンドを管理するための
        詳細については<smbconfoption name="idmap config"/>を参照のこと。
	</para>
</description>

<value type="example">default AD CORP</value>
</samba:parameter>
