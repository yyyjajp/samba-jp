<samba:parameter name="async dns timeout"
                 context="G"
                 type="integer"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>
    Samba 中の、非同期 DNS リゾルバコードが応答を待つときの秒数。
    ある種の Samba クライアントライブラリコードは、 Active Directory
    ドメインコントローラを見つけるため、A と AAAA レコードに対して、
    内部非同期 DNS 解決を使う。この値は、この名前解決コードが、
    DNS サーバのタイムアウトを待つことを防ぐ。
    </para>
    <para>このパラメータの最小値は固定されている。
    at 1 second.</para>
</description>

<value type="default">10</value>
<value type="example">20</value>
</samba:parameter>
