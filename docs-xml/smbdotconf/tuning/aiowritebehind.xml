<samba:parameter name="aio write behind"
                 context="S"
		 type="string"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>もしもSambaが非同期I/Oサポート込みで構築されている場合、
	  Sambaはこのパラメータ中で一覧表示されているファイルには、クライアントに
	  対して結果を返す前に、書き込み要求が終了する事を待たない。
	  その代わりに、Sambaは、操作が成功したか否かにかかわらず、書き込み操作が
	  問題なく完了したという結果をすぐに戻す。これは、非同期I/Oサポートを
	  しないクライアントを高速化するかもしれないが、データが失われる可能性が
	  あり、ファイルに障害を与える可能性があるため、とても危険である。
	</para>
	<para>
	  文法は、<smbconfoption name="veto files"/>パラメータと同じである。
	</para>
</description>

<value type="default"/>
<value type="example">/*.tmp/</value>
</samba:parameter>
