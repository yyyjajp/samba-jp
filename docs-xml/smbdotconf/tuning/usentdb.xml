<samba:parameter name="use ntdb"
                 context="G"
                                type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    Samba 4.1 からは、多くのデータベースが TDB に代わって NTDB 
    フォーマットを使用できる。
    </para>
  <para>
    このパラメーターを yes にした場合、これらのデータベースは
    デフォルトで、 <filename>.tdb</filename> に代わって
    <filename>.ntdb</filename> という拡張子を使用するようになる。
    既存の <filename>.tdb</filename> ファイルは、自動的に NTDB
    フォーマットに変換され、元のファイルは
    <filename>.tdb.bak</filename> にリネームされる。
    </para>
  <para>
    <filename>smb.conf</filename> でファイル名が明示的に指定
    された場合は、そちらが優先される (この場合のフォーマットは、拡張子が
    <filename>.ntdb</filename> か <filename>.tdb</filename> かに依存する) 。
  </para>
</description>

<value type="default">no</value>
</samba:parameter>
