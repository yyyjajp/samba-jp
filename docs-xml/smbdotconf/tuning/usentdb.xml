<samba:parameter name="use ntdb"
                 context="G"
                                type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>Samba 4.1から、多くのデータベースがTDBのかわりにNTDBフォーマットを
    使えるようになってきている。
    </para>
    <para>このオプションを有効にした場合、これらのデータベースは
    デフォルトで、<filename>.tdb</filename>のかわりに<filename>.ntdb</filename>
    という拡張子を使うようになる。<filename>.tdb</filename>
    ファイルが存在する場合、自動的にNTDBフォーマットに変換され、元のファイルは
    <filename>.tdb.bak</filename>にリネームされる。
    </para>
    <para><filename>smb.conf</filename>で明示的にファイル名を指定
    された場合は、そちらが優先される（すなわち、フォーマットは、拡張子が
    <filename>.ntdb</filename>か<filename>.tdb</filename>かに依存する）。
    </para>
</description>

<value type="default">no</value>
</samba:parameter>
