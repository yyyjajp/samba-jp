<samba:parameter name="use ntdb"
                 context="G"
                                type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>Samba 4.1から、多くのデータベースがTDBのかわりにNTDB形式を
    使えるようになっている。
    </para>
    <para>もしもこのオプションを有効にした場合、これらのデータベースは
    既定値で、<filename>.tdb</filename>のかわりに、<filename>.ntdb</filename>
    という拡張子を使うようになる。もしも、 <filename>.tdb</filename>
    ファイルが存在する場合、自動的にNTDB形式に変換され、それは
    <filename>.tdb.bak</filename>に改名される。
    </para>
    <para>明示的に<filename>smb.conf</filename>でファイル名を指定
    されたものは、それが有効となる(すなわち、形式は、
    <filename>.ntdb</filename>か<filename>.tdb</filename>拡張子に
    依存する)。
    </para>
</description>

<value type="default">no</value>
</samba:parameter>
