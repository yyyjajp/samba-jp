<samba:parameter name="strict allocate"
                 context="S"
		 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>この真偽値パラメータは、サーバ上のディスク領域の割当を制御するものである。
		このパラメータが <constant>yes</constant> の場合、
		サーバは、ファイルが拡張されただけでは実際のディスク領域を割り当てない UNIX 流の動作をやめ、
		ファイルがあるサイズで作成されたり、拡張されたりした際に、
		実際にディスク領域を割り当てる Windows 流の動作を行なうようになる。
		これは、Samba がスパースファイルを作成しなくなることを意味する。
		これにより、システムによっては処理が遅くなる。</para>

		<para>strict allocate が <constant>no</constant> の場合、
		サーバは、ファイルの拡張時にスパースファイルを作成する。</para>

		<para>このパラメータを <constant>yes</constant> にすると、
		ディスククオータによるディスク領域の制限を行なっているシステムで、
		クォータ超過メッセージを出力する際には便利である。</para>
</description>
<value type="default">no</value>
</samba:parameter>
