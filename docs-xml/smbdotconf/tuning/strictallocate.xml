<samba:parameter name="strict allocate"
                 context="S"
		 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>この真偽値パラメーターは、サーバー上のディスク領域の割当を制御するものである。
		このパラメーターが <constant>yes</constant> の場合、
		サーバーは、ファイルが拡張されただけでは実際のディスク領域を割り当てない UNIX 流の動作をやめ、
		ファイルがあるサイズで作成されたり、拡張されたりした際に、
		実際にディスク領域を割り当てる Windows 流の動作を行なうようになる。
		これは、Samba がスパースファイルを作成しなくなることを意味する。</para>

		<para>このオプションは、例えばエクステントベースのファイルシステムの
		ような大量ブロックの高速割り当てをサポートするファイルシステムのために、
		まさに設計されたオプションである。エクステントをサポートしない
		ファイルシステム(代表的なものはext3)上では、Sambaの処理が遅くなる
		場合がある。エクステントがないファイルシステム上で、100MB超のような
		大きなファイルを扱っている場合、クライアントがタイムアウトになって
		しまうような 問題に遭遇するかもしれない。</para>

		<para>エクステントベースのファイルシステムを使っている場合、
		大量の空間を迅速に割り当てることがSambaで出来る、未書き込みのエクステントを
		使う事が出来、strict allocateによって引き起こされた何らかのタイムアウト
		問題が発生しなくなるだろう。strict allocateを使っているとき、
		quotaを使っている場合は、quotaからのより的確なメッセージが得られるだろう。
		この設定を有効にすることによる、その他の利点は、ファイルの断片化を
		減少する助けになるということである。</para>

		<para>ファイルシステム上での注目する点を提供するために、この設定は、
		現時点でよいオプションであるかもしれない:Linux上でのXFS、ext4、btrfs、
		ocfs2と、AIX上でのjfsは、未書き込みのエクステントをサポートする。
		それらをサポートしないファイルシステム上では、事前割当は、おそらく、
		パフォーマンスが低下する負荷のかかる操作であり、巨大なファイルを作成
		するときにクライアントがタイムアウトになる危険性がある。例は、
		Ext3、ZFS、HFS+とその他のものであり、それらのファイルシステム上で
		この設定を有効にするときには気をつけること。</para>
</description>
<value type="default">no</value>
</samba:parameter>
