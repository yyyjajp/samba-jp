<samba:parameter name="strict rename"
                 context="S"
				 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <pala>既定値では、Windows SMB サーバは、ファイルシステム階層構造中で、
  そのディレクトリ配下のファイルまたはディレクトリがオープンされている場合、
  ディレクトリの名前変更ができない。POSIXファイルシステムの規定が要求するので、
  歴史的な理由により、Samba は 常時この機能が有効である。</para>

  <para>この真偽値パラメータは Samba に Windows の動作と一致するように
  させる。"yes"に設定した場合、ハンドルデータベース全体で、すべての
  ディレクトリ名前変更要求を、Sambaに調べさせるため、とても負荷のかかる
  変更となる。クラスタ化されたSambaシステムでは、非クラスタ化された
  場合よりも更に負荷がかかる。</para>

  <para>上記理由により、既定値は "no" であり、特定の Windows アプリケーション
  が、変更要求を出さない限り、そのままにしておくことを推奨する。</para>
</description>

<value type="default">no</value>
</samba:parameter>
