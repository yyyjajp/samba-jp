<samba:parameter name="ldap follow referral" context="G" type="enum"
	advanced="1" developer="1"
	xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    このパラメーターは、 LDAP データベースからエントリを検索する際に、
    LDAP referral を使用するかどうかを制御する。取り得る値は
    referral 機能を使用する場合の <emphasis>on</emphasis> 、
    これを無効にする場合の <emphasis>off</emphasis> 、
    libldap のデフォルトの設定を使用する場合の <emphasis>auto</emphasis> である。
    libldap が referral 機能を使用するかどうかは、 ldap.conf(5) で記述
    されているとおり、 /etc/openldap/ldap.conf の REFERRALS パラメーターで
    設定される。
  </para>
</description>

<value type="default">auto</value>
<value type="example">off</value>

</samba:parameter>
