<samba:parameter name="ldap follow referral"
		 context="G"
		 type="enum"
		 enumlist="enum_bool_auto"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">

<description>

	<para>このオプションは、LDAP データベース中でエントリを検索する時に、
	  LDAP referral を利用するかどうかを制御する。設定可能な値は、
	  referral 機能の利用を有効にする場合は、<emphasis>on</emphasis>、
	  これを無効にする場合は、<emphasis>off</emphasis>、
	  libldap の既定値の設定を使う場合は<emphasis>auto</emphasis>である。
	  libldap による refarral 機能の仕様の有無は、ldap.conf(5) 中に記述がある、
	  REFERRALS パラメーターによって、/etc/openldap/ldap.conf 中で
	  設定される。</para>

</description>

<value type="default">auto</value>
<value type="example">off</value>

</samba:parameter>
