<samba:parameter name="ldap timeout"
                 context="G"
                 advanced="1" developer="1"
				 type="integer"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>
	Samba が LDAP サーバに接続しようとした際に、該当サーバがダウンしていたり、該当サーバに到達することができなかったりした場合に
	Samba が接続待ちでフリーズしてしまわないように、このパラメータでは Samba が接続の確立を待機する時間を秒単位で指定する。
	デフォルトは 15 秒であり、この時間内に LDAP サーバは接続要求に対して応答する必要がある。
	</para>
</description>
<value type="default">15</value>
</samba:parameter>
