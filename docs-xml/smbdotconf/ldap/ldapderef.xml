<samba:parameter name="ldap deref" context="G" type="enum"
	advanced="1" developer="1"
	xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">

<description>
  <para>
    このパラメーターは、 Sambaが LDAPライブラリに対して別名 (alias) を
    参照する方法を指定するかどうかを制御する。
    デフォルトは <emphasis>auto</emphasis>
    であり、これは LDAP クライアントライブラリのデフォルトの設定が
    そのまま使用されることを意味する。これ以外に取り得る値としては、
    <emphasis>never</emphasis> 、 <emphasis>finding</emphasis> 、
    <emphasis>searching</emphasis> 、 <emphasis>always</emphasis> がある。
    詳細な情報は、使用している LDAP のマニュアルから取得してほしい。
  </para>

</description>

<value type="default">auto</value>
<value type="example">searching</value>

</samba:parameter>
