<samba:parameter name="ldap ssl"
                 context="G"
				 type="enum"
                 advanced="1" developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このオプションは、Samba が ldap server に接続する際に、SSL
	を用いるかどうかを制御するために用いられる。これは、
	<filename moreinfo="none">configure</filename> スクリプトの
	<command moreinfo="none">--with-ssl</command> オプションで有効化される、
	Samba の古い SSL サポートとは <emphasis>無関係</emphasis></para>である。
		
	<para><parameter moreinfo="none">ldap ssl</parameter> の値には、以下の
	3 つのうちいずれかを設定すること:</para>	
	<itemizedlist>
		<listitem>
			<para><parameter moreinfo="none">Off</parameter> = 
			ディレクトリのクエリ時には決して SSL を用いない。
			</para>
		</listitem>

		<listitem>
			<para><parameter moreinfo="none">Start_tls</parameter>
			= ディレクトリサーバと通信する際に、LDAPv3 StartTLS
			拡張オプション (RFC2830) を利用する。</para>
		</listitem>
	    
		<listitem>
			<para><parameter moreinfo="none">On</parameter> = 
			<parameter moreinfo="none">ldap server</parameter>
			に対して、常に ldaps ポートで SSL を用いて接続する。
			下位互換である <command moreinfo="none">--with-ldapsam
			</command> オプションが configure 時に指定された場合に
			のみ有効である。
			<smbconfoption name="passdb backend"/> も参照のこと。</para>
		</listitem>
	</itemizedlist>		
</description>
<value type="default">start_tls</value>
</samba:parameter>
