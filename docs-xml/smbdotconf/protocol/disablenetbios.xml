<samba:parameter name="disable netbios"
                 context="G"
		 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>このパラメーターを有効にすると、 Samba の NetBIOS サポート機能が無効になる。
    Windows 2000 より後のバージョンの Windows 
    では、NetBIOS のみを使用できる。</para>

    <note><para>NetBIOS サポートを無効にすると、 
    NetBIOS のみをサポートするクライアントからは Samba サーバーにアクセスできなくなることに注意。
    </para></note>
</description>
<value type="default">no</value>
</samba:parameter>
