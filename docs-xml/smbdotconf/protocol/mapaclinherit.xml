<samba:parameter name="map acl inherit"
                 context="S"
		 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>この真偽値パラメーターにより、 <citerefentry><refentrytitle>smbd</refentrytitle>                                       
    <manvolnum>8</manvolnum></citerefentry> は
    Windows の ACL に格納される「inherit」および「protected」の ACE フラグを user.SAMBA_PAI
    (POSIX ACL インタフェース)と呼ばれる拡張属性にマッピングすることが可能となる。
    このパラメーターはファイルシステムで拡張属性をサポートすることを必要とし、
    NT ACLがベストエフォートで POSIX ACLにマッピングされている時、
    Windows の ACL エディタが、継承を格納できるようにする。
    </para>
</description>
<value type="default">no</value>
</samba:parameter>
