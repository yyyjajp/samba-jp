<samba:parameter name="rpc big endian"
                 context="G"
				 type="bool"
                 advanced="1" developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    このパラメーターを設定すると、RPC クライアントとサーバー間での
	データ転送がビッグエンディアンで行われるようになる。</para>

	<para>無効にした場合、データ転送はリトルエンディアンで行われる。</para>

	<para>この動作は、ホストマシンのエンディアンには依存しない。</para>
</description>

<value type="default">False</value>
</samba:parameter>
