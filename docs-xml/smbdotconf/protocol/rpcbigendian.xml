<samba:parameter name="rpc big endian"
                 context="G"
				 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このオプションを設定すると、RPC クライアントとサーバー間での
	転送データをビッグエンディアンで行うようにさせる。</para>

	<para>無効にした場合には、データ転送はリトルエンディアンで行う。</para>

	<para>動作は、ホストマシンのエンディアン状態に依存しない。</para>
</description>

<value type="default">no</value>
</samba:parameter>
