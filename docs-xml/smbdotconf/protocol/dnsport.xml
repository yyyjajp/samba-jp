<samba:parameter name="dns port"
                 context="G"
                 type="integer"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>サーバが DNS トラフィックを監視するポートを指定する。
    </para>

    <para>これにより、他の DNS サーバを Samba のフロントにして、フォワード
    として使用させることが可能になる。</para>

    <warning>
        <para>動的 DNS 更新は、Samba へのフォワード時に、フロント DNS
        サーバによってプロキシされない場合がある。動的 DNS 更新 プロキシは、
	フロントとして使われる他の DNS サーバの機能に依存する。</para>
    </warning>
</description>

<value type="default">53</value>
</samba:parameter>
