<samba:parameter name="server min protocol"
                 context="G"
                 type="enum"
                 developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<synonym>min protocol</synonym>
<description>
	<para>この設定は、クライアントが使う時にサーバが許可する最低のプロトコル
	バージョンを制御する。</para>

	<para>通常このオプションは、SMBプロトコルが自動的にネゴシエーションする
    段階において、適切なプロトコルをちゃんと選ぶため、設定すべきではない。</para>
</description>

<related>server max protocol</related>

<value type="default">CORE</value>
<value type="example">NT1</value>
</samba:parameter>
