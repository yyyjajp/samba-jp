<samba:parameter name="server min protocol"
                 context="G"
                 type="enum"
                 developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<synonym>min protocol</synonym>
<description>
	<para>この設定は、サーバーがクライアントに許可する最低位のプロトコル
	を制御する。</para>

    <para>通常は、SMB プロトコルの自動ネゴシエーションフェーズにより
    適切なプロトコルが選択されるため、このオプションを設定する必要はない。

      <para>有効なプロトコルの完全な一覧については<related>server max protocol</related>
      を参照のこと。</para>
</description>

<related>server max protocol</related>

<value type="default">LAMNAN1</value>
<value type="example">NT1</value>
</samba:parameter>
