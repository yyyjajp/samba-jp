<samba:parameter name="server multi channel support"
                 context="G"
                 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>この真偽値パラメータは、
    <citerefentry><refentrytitle>smbd</refentrytitle>
    <manvolnum>8</manvolnum></citerefentry> が、
    SMB3 multi-channel をサポートするかどうかを制御する。
    </para>
    <para>このパラメータはバージョン 4.4 で追加された。</para>
<!-- Samba 4.15.0
    <para>
    Note that this feature was still considered experimental up to 4.14.
    </para>
-->	 
    <para>
     この機能は、4.14 まではまだ実験的と見なされていたことに注意。
    </para>

<!-- Samba 4.13.0
    <para>Due to dependencies to kernel APIs of Linux or FreeBSD, it's only possible
    to use this feature on Linux and FreeBSD for now. For testing this restriction
    can be overwritten by specifying <constant>force:server multi channel support=yes</constant>
    in addition.</para>
 -->
    <para>Linux 又は FreeBSD の カーネル API に依存するため、この機能は、現時点では
    Linux と FreeBSD でのみ使用可能である。テストのため、この制限は、追加で、
    <constant>force:server multi channel support=yes</constant> を指定することで
    上書きできる。</para>

<!-- Samba-4.15.0
     <para>
     This option is enabled by default starting with to 4.15 (on Linux and FreeBSD).
     </para>
-->
     <para>
     このオプションは、4.15 からは、既定で有効になっている(Linux および FreeBSDの場合)。
     </para>

</description>

<value type="default">no</value>
</samba:parameter>
