<samba:parameter name="unix extensions"
                 context="G"
				 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
 <description>
    <para>この真偽値パラメーターは、Samba が 
    HP 社によって定義された CIFS の UNIX 拡張を利用するかどうかを制御する。
    これらの拡張は、UNIX クライアントから UNIX サーバーに対して接続する際に有用な CIFS を有効にし、
    シンボリックリンクやハードリンクなどを利用可能にする。
    これらの拡張により、Samba は UNIX の CIFS クライアントに対して、
    シンボリックリンクやハードリンクといった機能を提供することが可能となる。</para>
    <para>
      もしも2つのパラメーター間での連携を変更したいのであれば、
    　<smbconfoption name="allow insecure wide links"/>を参照のこと。
    </para>
</description>
<value type="default">yes</value>
</samba:parameter>
