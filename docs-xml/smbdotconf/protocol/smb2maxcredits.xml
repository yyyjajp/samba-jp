<samba:parameter name="smb2 max credits"
		type="integer"
		context="G"
		advanced="1" developer="1"
		xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
<para>このオプションは、Sambaがクライアントに対して許可をする、最大同時SMB2操作
数を制御する。これは、SMB1の<smbconfoption name="max mux"/>と同じようなものである。
このパラメーターを設定する必要は決してないだろう。
This option controls the maximum number of outstanding simultaneous SMB2 operations
that Samba tells the client it will allow. This is similar to the <smbconfoption name="max mux"/>
parameter for SMB1. You should never need to set this parameter.
</para>
<para>既定値は128であり、これは、Windows SMB2サーバーと同じである。</para>
</description>

<value type="default">128</value>
</samba:parameter>
