<samba:parameter name="smb2 max credits"
		type="integer"
		context="G"
		handler="handle_smb2_max_credits"
		xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
<!-- samba 4.7.0
<para>
This option controls the maximum number of outstanding simultaneous SMB2 operations
that Samba tells the client it will allow. This is similar to the <smbconfoption name="max mux"/>
parameter for SMB1. You should never need to set this parameter.
</para>
-->

<para>このオプションは、Samba がクライアントに対して許可する、最大同時 SMB2 操作
数を制御する。これは、SMB1 の<smbconfoption name="max mux"/>と同じようなものである。
このパラメーターを設定する必要は決してないだろう。</para>

<!-- samba 4.7.0
<para>The default is 8192 credits, which is the same as a Windows 2008R2 SMB2 server.</para>
-->

<para>既定値は8192であり、これは、Windows Windows 2008R2 SMB2 サーバーと同じである。</para>
</description>

<value type="default">8192</value>
</samba:parameter>
