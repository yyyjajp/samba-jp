<samba:parameter name="client min protocol"
                 context="G"
                 type="enum"
				 developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    このパラメーターは、クライアントが使用を試みる最低位のプロトコル
    を指定する。
  </para>

  <para>
    通常は、SMB プロトコルの自動ネゴシエーションフェーズで
    適切なプロトコルが選択されるため、このパラメーターを設定する必要はない。
  </para>

  <para>
    設定できるプロトコルの一覧については <related>client max protocol</related>
    を参照のこと。
  </para>
</description>

<related>client max protocol</related>
<related>server min protocol</related>
<value type="default">CORE</value>
<value type="example">NT1</value>
</samba:parameter>
