<samba:parameter name="ncalrpc dir"
                 context="G"
                 advanced="1" developer="1"
		 type="string"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このディレクトリは、プロセス間通信でRPCを許可するために
	一連の名前付きパイプを保持するディレクトリである。
This directory will hold a series of named pipes to allow RPC over inter-process communication.  </para>.
	<para>これは、Samba と他の unix プロセスに、 TCP/IP を使わないで DCE/RPC
	超えで通信を行わせることを許可する。さらに追加で、サブディレクトリ
	 'np' は、アクセス許可が制限され、Samba プロセス間での信頼された通信
	 チャネルを許可する。
This will allow Samba and other unix processes to interact over DCE/RPC without using TCP/IP.  Additionally a sub-directory 'np' has restricted permissions, and allows a trusted communication channel between Samba processes</para>
</description>

<value type="default">${prefix}/var/ncalrpc</value>
<value type="example">/var/run/samba/ncalrpc</value>
</samba:parameter>
