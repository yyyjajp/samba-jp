<samba:parameter name="log writeable files on exit"
		 context="G"
		 type="boolean"
		 advanced="1" developer="0"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>
	  CIFSクライアントとSambaとの間での接続が切れた時、Sambaは
	  サーバーサイドのネットワーク接続を単に停止する以外のオプションは
	  持っていない。もしもこれが起きた場合、Windowsアプリケーションが
	  要求したすべての書き込み動作を、Windowsクライアントが完了していない
	  ために、データの破壊の危険性がある。このオプションを"yes"に設定
	  すると、smbdは、ネットワーク接続が切断された時に書き込みモードで
	  オープンしていたすべてのファイルの一覧をログレベル0のメッセージ
	  としてログ出力する。これらはデータ破壊の可能性があるファイルである。
	  これは、整合性チェックを行うファイルの一覧を、管理者に与える手段
	  という意味がある。
	</para>
</description>

<value type="default">no</value>
</samba:parameter>
