<samba:parameter name="async smb echo handler"
                 context="G"
		 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメーターは、Samba が非同期 smb エコーハンドラーを
	フォークすべきかを指定する。もしも使用するファイルシステムが
	とても長い時間システムコールをブロックするのであれば、これは
	便利に使える。ある種の環境では、接続が切れてしまったどうかを
	決めるために、Windows が使うタイムアウトを延長する。このパラメータは
	SMB1専用である。SMB2またはそれ以降については、代わりに
	TCP キープアライブ を使う事ができる。
	</para>
</description>
<value type="default">no</value>
</samba:parameter>
