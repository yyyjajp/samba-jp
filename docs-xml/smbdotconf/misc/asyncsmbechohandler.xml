<samba:parameter name="async smb echo handler"
                 context="G"
				 type="boolean"
                 advanced="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメータは、Samba が非同期 smb エコーハンドラを
	フォークすべきかを指定する。もしも使用するファイルシステムが
	とても長い時間システムコールをブロックするのであれば、これは
	便利に使える。ある種の環境では、接続が切れてしまったどうかを
	決めるために、Windows が使うタイムアウトを延長する。
	</para>
</description>
<value type="default">no</value>
</samba:parameter>
