<samba:parameter name="directory name cache size"
		 context="S"
		 type="integer"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>
	  このパラメーターは、SMB1 接続時の、ディレクトリネームキャッシュのサイズを指定する。
	  SMB2 では使われない。これは *BSD システムでは off にする必要がある。
	</para>

</description>
<value type="default">100</value>
</samba:parameter>
