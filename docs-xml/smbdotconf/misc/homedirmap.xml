<samba:parameter name="homedir map"
	         context="G"
			 type="string"
		 advanced="1" developer="1"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
 <description>
	<para>このパラメータは、
	<smbconfoption name="nis homedir"/> が <constant>yes</constant> で
	<citerefentry><refentrytitle>smbd</refentrytitle>
	<manvolnum>8</manvolnum></citerefentry> が
	Windows 95/98 の <parameter moreinfo="none">logon server</parameter> として機能している場合に、
	ユーザのホームディレクトリがあるサーバがどこかの情報をもつ NIS (YP)のマップを指定する。
	現在のところ、Sun の auto.home マップ形式のみが認識される。
	マップの形式は以下のとおり:
<programlisting>
<command moreinfo="none">username server:/some/file/system</command>
</programlisting>
	プログラムは、最初の「:」の前までをサーバ名として解釈する。
	これ以外のマップの形式や、Amd (別の automounter)のマップにも対応した優れた解析ロジックもおそらく存在するはずである。
	</para>
		
	<note><para>このオプションを利用する場合は、
	システムがNISクライアントとして稼働している必要がある。</para></note>
</description>
<related>nis homedir</related>
<related>domain logons</related>

<value type="default"></value>
<value type="example">amd.homedir</value>
</samba:parameter>
