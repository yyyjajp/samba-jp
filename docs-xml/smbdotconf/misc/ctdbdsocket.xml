<samba:parameter name="ctdbd socket"
                 context="G"
                 advanced="1"
				 type="string"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    <value type="example">clustering=yes</value> を指定する場合は、
    ctdbd が待ち受ける unix ドメインのソケットを Samba に設定する必要が
    ある。 ctdb 1.0 でのデフォルトのパスは /tmp/ctdb.socket だが、
    smb.conf で明示的に設定する必要がある。
  </para>
</description>
<value type="default"></value>
<value type="example">/tmp/ctdb.socket</value>
</samba:parameter>
