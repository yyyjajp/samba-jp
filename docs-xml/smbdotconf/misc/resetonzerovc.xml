<samba:parameter name="reset on zero vc"
		 context="G"
		 type="boolean"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>
	この真偽値オプションは、到着した SMB1 のセッションセットアップが、
	同一の IP アドレスからの別のセッションを切断するかを制御する。
	これは Windows Server 2003 におけるデフォルトの挙動である。

	ネットワークが不安定で、 Windows が、古い接続がファイルを共有モードでオープンしているにも関わらず、再接続を試行してしまうような環境では、このパラメーターを yes にする必要があるだろう。
	これらのファイルは、新しい接続経由ではアクセスできなくなるが、
	クライアントが新しい接続中で 0 VC を送信すると、Windows Server 2003 は同一の IP が送信元のすべての他の接続を切断する。
	これにより、ロックされたファイルが再度アクセスできるようになる。

	このオプションを有効にすると、ルータで同一 IP アドレスにマスカレード
	されている接続も切断されてしまうことに注意。また、SMB2 または SMB3
	のみを使ったクライアントには影響しない。
	</para>
</description>

<value type="default">no</value>
</samba:parameter>
