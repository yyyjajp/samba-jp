<samba:parameter name="fam change notify"
                 context="G"
				 type="boolean"
                 advanced="1" developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメータにより、Samba がディレクトリ内の変更を
	FAM デーモンに問い合わせ、SMB クライアントがサーバ上のデータに変更があった際に、
	更新を行なうことが可能になるようにするかが制御される。
	</para>

	<para>このパラメータは、FAM デーモンによるユーザプログラムへの変更通知機能がサポートされている場合のみ使用される。
	FAM デーモンが実行されていない場合、このパラメータは自動的に無効となる。
	<parameter moreinfo="none">kernel change notify</parameter>
	パラメータが有効な場合、そちらが優先される。
	</para>
</description>
<value type="default">yes</value>
</samba:parameter>
