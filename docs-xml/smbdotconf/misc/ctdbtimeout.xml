<samba:parameter name="ctdb timeout"
                 context="G"
		 type="integer"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメーターは Samba と ctdb 間での接続のタイムアウト時間をミリ秒単位で指定する。
	  これは Samba をクラスターリングがサポート出来るようにコンパイルし、
	  <parameter>clustering=yes</parameter>を設定した場合にのみ有効である。
	</para>
	<para>クラスターブロック内に何らかのものがある場合、ブロック状態に追加しようと
	  するときに、ctdb をとても長い時間待つことがたまにある。うまく動作するクラスター
	  環境においては、これは決して発生しないが、ちょっとした問題があるかもしれない
	  とてもたくさんのコンポーネントがクラスター中にはある。クラスター間で何かを転送
	  するために、長いサービス時間がかかる、負荷の高いクラスター上では有効かもしれない
	  という理由で、この値の正しい落とし所を選択するのはとてもトリッキーである。
	  この値を小さくしすぎると、クラスターが提供するサービスをデグレードする事になり、
	  長くしすぎると、クラスターそれ自身を、何らかの重大な障害から、長い時間リカバリ
	  できなくなるかもしれない。
	</para>
	<para>
	  もしも、このパラメーターを設定する場合、これは smb.conf 中に存在する必要があるが、
	  レジストリに接続するのに ctdb が必要であるという理由で、レジストリ設定中
	  (通常はクラスター上)に配置するのは好ましくないことに注意。
	</para>
	<para><parameter>ctdb timeout</parameter>を n に設定することは、クラスターのパニック
	  による応答に、どのプロセスも n ミリ秒以上待たないということになる。既定値として強く
	  推奨する0に設定(既定値)すると、Samba はそれを永久にブロックする。
	</para>
</description>
<value type="default">0</value>
</samba:parameter>
