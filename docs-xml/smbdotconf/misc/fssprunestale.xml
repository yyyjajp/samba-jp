<samba:parameter name="fss: prune stale"
                 context="G"
		 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
  <description>
    <para>
      これを有効にした場合、Samba のファイルサーバリモートVSSプロトコル
      (FSRVP)サーバは、すべての、FSRVPが、スタートアップ時にスナップショット
      を開始したかを確認し、存在しないスナップショットパスに
      対応するステート(共有定義も含む)を削除する。
    </para>
	</para>
</description>
<value type="default">no</value>
<value type="example">yes</value>
</samba:parameter>
