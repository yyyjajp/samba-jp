<samba:parameter name="utmp directory"
                 context="G"
				 type="string"
                 advanced="1" developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメーターは、
	Sambaが <command moreinfo="none">--with-utmp</command> オプションで
	configure され、コンパイルされた時にのみに有効である。
	このパラメーターは、
	Sambaサーバーへの接続を記録する utmp または utmpx ファイル
	(UNIXシステムに依存)が存在するディレクトリを指定する。
	デフォルトではこの値は設定されない。
	この場合、システムが利用している utmp ファイルが使われる
	(Linux の場合、通常 
	<filename moreinfo="none">/var/run/utmp</filename> にある)。</para>
</description>
<related>utmp</related>
<value type="default"><comment>Determined automatically</comment></value>
<value type="example">/var/run/utmp</value>
</samba:parameter>
