<samba:parameter name="utmp directory"
                 context="G"
		 type="string"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメーターは、
	Samba が <command moreinfo="none">--with-utmp</command> オプションで
	構成され、コンパイルされた時にのみに有効である。
	このパラメーターは、
	Samba サーバーへの接続を記録する utmp または utmpx ファイル
	(UNIX システムに依存)が存在するディレクトリを指定する。
	デフォルトではこの値は設定されない。
	この場合、システムが利用している utmp ファイルが使われる
	(Linux の場合、通常 
	<filename moreinfo="none">/var/run/utmp</filename> にある)。</para>
</description>
<related>utmp</related>
<value type="default"><comment>自動的に決定される</comment></value>
<value type="example">/var/run/utmp</value>
</samba:parameter>
