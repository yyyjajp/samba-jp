<samba:parameter name="state directory"
                 context="G"
                 advanced="1" developer="1"
		 type="string"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    通常、 TDB ファイルのほとんどは、
    <parameter moreinfo="none">lock directory</parameter>
    内に配置されるが、 Samba 3.4.0 からは、
    <parameter moreinfo="none">state directory</parameter> と
    <parameter moreinfo="none">cache directory</parameter> により、
    恒常的なデータを保持する TDB ファイルと、一時的なデータを保持する
    TDB ファイルとを分離することが可能となった。
  </para>

  <para>
    このパラメーターは、恒常的なデータを保持する TDB ファイルが
    配置されるディレクトリを指定する。
  </para>
</description>

<value type="default">${prefix}/var/locks</value>
<value type="example">/var/run/samba/locks/state</value>
</samba:parameter>
