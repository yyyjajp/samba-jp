<samba:parameter name="fss: sequence timeout"
                 context="G"
		 type="integer"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
  <description>
    <para>
      ファイルサーバリモート VSS プロトコル(FSRVP)サーバは、予期しないクライアントからの
      切断時にクリーンアップを確実に行うための、メッセージシーケンスタイマを組込む。
      このパラメータは、FSRVP 操作間での既定値のタイムアウトを上書きする。
      FSRVP タイムアウトは、値を 0 にする事で、完全に無効化できる。
    </para>
</description>
<value type="default">180 又は 1800 で、操作に依存する</value>
<value type="example">0</value>
</samba:parameter>
