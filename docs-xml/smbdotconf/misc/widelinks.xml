<samba:parameter name="wide links"
		 context="S"
		 type="boolean"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメーターは、
	UNIXファイルシステム上のリンクをサーバーが追跡するかどうかを制御する。
	サーバーが公開するディレクトリツリー内部へのリンクは、
	常に許可されている。このパラメーターは、
	公開されているディレクトリツリー外部へのアクセスを制御する。</para>

	<para>注意:UNIX extensionが有効の時にこのパラメータをonにすると、
	共有定義によってエクスポートされた、制限されたパスの外側に
	対する、ファイルやディレクトリのポインタを共有上に、シンボリックリンク
	としてUNIXクライアントが作成できるようになる。これにより、共有外部の
	領域に対してアクセス出来るようになる。この問題のため、このパラメータは
	<smbconfoption name="unix extensions"/>オプションがonの場合は、
	(その旨をログに記録して)自動的に無効となる。
	</para>
	<para>
	  2つのパラメータ間での連携を変更したいのであれば、
	  <smbconfoption name="allow insecure wide links"/>パラメータも
	  参照のこと。
	</para>

</description>
<value type="default">yes</value>
</samba:parameter>
