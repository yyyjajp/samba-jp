<samba:parameter name="wide links"
		 context="S"
		 type="boolean"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメータは、
	UNIXファイルシステム上のリンクをサーバが追跡するかどうかを制御する。
	サーバが公開するディレクトリツリー内部へのリンクは、
	常に許可されている。このパラメータは、
	公開されているディレクトリツリー外部へのアクセスを制御する。</para>

	<para>注意:UNIX拡張が有効な時にこのパラメータを有効にすると、共有の
	  定義でエクスポートされた、制限されている領域の外側にある、ファイルや
	  ディレクトリを指定する事が出来る、共有上のシンボリックリンクを作成
	  することができる。これにより、共有の外側の領域にアクセス出来るように
	  なる。この問題のため、
	  <smbconfoption name="unix extensions"/>パラメータがonの場合、
	  このパラメータは(ログファイルにその旨を通知して)自動的に無効になる。
	</para>

</description>
<value type="default">no</value>
</samba:parameter>
