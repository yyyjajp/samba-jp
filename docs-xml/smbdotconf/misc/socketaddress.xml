<samba:parameter name="socket address"
	         context="G"
			 type="string"
		 developer="1"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このオプションは、
	Samba がどのアドレスで接続を listen するかを設定する。
	これは、サーバ上で複数の仮想インタフェースを用いる必要があり、
	各々のインタフェース上で 異なる構成の Samba を動作させる際に利用できる。
	</para>		

	<para>通常はnmbdは1つしか起動しないため、このオプションを指定する必要はない。</para>

	<para>既定値では、Sambaは全てのアドレスへの接続を受け付ける。</para>
		
</description>

<value type="default"></value>
<value type="example">192.168.2.20</value>
</samba:parameter>
