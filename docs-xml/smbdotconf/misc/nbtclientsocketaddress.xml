<samba:parameter name="nbt client socket address"
	         context="G"
			 type="string"
		 developer="1"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<synonym>socket address</synonym>
<description>
  <para>
    このパラメーターは、 nmbd を含む Samba が NBT パケットの送受信に
    使用するアドレスを制御する。
  </para>
  <para>
    このパラメーターは、 nmbd が多重実行されていない、一般的な
    Samba サーバでは設定不要である。
  </para>
  <para>
    Samba のデフォルトでは、 OS のデフォルトのアドレスから UDP パケットを
  送信し、0.0.0.0 で受信する。
  </para>
  <para>
    このパラメーターは廃止予定である。通常の待ち受けアドレスを制御する
    方法についての、 <smbconfoption name="bind interfaces only">
    yes</smbconfoption> と <smbconfoption name="interfaces"></smbconfoption>
    も参照のこと。
  </para>
</description>

<value type="default">0.0.0.0</value>
<value type="example">192.168.2.20</value>
</samba:parameter>
