<samba:parameter name="mangled map"
				 type="string"
                 context="S"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para> このパラメータは、
	Windows や DOS で表示できない UNIX ファイル名の対応づけを直接指定するためのものである。
	名前の短縮のみがこの機能に必要とされるすべてではない。
	実際、DOS と UNIX とでファイル名の拡張子の異なるドキュメントが存在することがある。
	一例を挙げると、UNIX では通常 HTML ファイルを示す拡張子として
 	 <filename moreinfo="none">.html</filename> が使用されるが、DOS では <filename moreinfo="none">.htm</filename> の方が一般的である。</para>

	<para> このため、<filename moreinfo="none">html</filename> と <filename moreinfo="none">htm</filename> 
	の対応づけを行なう場合は、以下のようにする:</para>
		
	<para>
	<smbconfoption name="mangled map">(*.html *.htm)</smbconfoption>.
	</para>

	<para> とても便利な利用方法の一つに、CDROMによってファイル名の末尾に付加されている
	<filename>;1</filename>(幾つかのUNIX上でのみ表示される)
	の削除が挙げられる。これを行なうためには、 (*;1 *;) のように指定すればよい。</para>
</description>

<value type="default"><comment>mangled map なし</comment></value>
<value type="example">(*;1 *;)</value>
</samba:parameter>
