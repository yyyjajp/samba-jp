<samba:parameter name="server services"
                 context="G"
                 type="list"
                 advanced="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このオプションは、Sambaデーモンが提供するサービスを指定する。</para>

	<para><filename>smb.conf</filename>で値を設定する場合は、
        それ以前の設定を完全に上書きすることもできるが、プレフィックスとして
        <constant>+</constant>または
	<constant>-</constant>を付加することで、サービスの追加、削除を個別に
        行うこともできる。
	</para>
</description>

<value type="default">s3fs rpc nbt wrepl ldap cldap kdc drepl winbind ntp_signd kcc dnsupdate dns</value>
<value type="example">-s3fs +smb</value>
</samba:parameter>
