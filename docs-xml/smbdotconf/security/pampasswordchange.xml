<samba:parameter name="pam password change"
                 context="G"
				 type="boolean"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para> Samba 2.2 における PAM サポート機能の追加にともない、
    このパラメーターを用いることで、 Samba で PAM を使ったパスワード変更が可能となった。
    このパラメーターが有効な場合、SMB クライアントからの要求があれば、
    <smbconfoption name="passwd program"/>. 
    に設定されたプログラムに代わって、PAM を使ってパスワードの変更が行なわれる。
    ほとんどの場合、
    <smbconfoption name="passwd chat"/> 
    パラメーターの変更なしにこのパラメーターを有効にすることが可能である。
    </para>
</description>

<value type="default">no</value>
</samba:parameter>
