<samba:parameter name="invalid users"
                 context="S"
				 type="list"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>これは、サービスに対するログインを許可しないユーザーのリストを指定する。
    このパラメーターは、不適切な設定により発生しうるセキュリティ上の問題を完全に抑止する、
    <emphasis>パラノイド的</emphasis>設定である。
    </para>
		
    <para>「@」ではじまる名前は(システムがNISをサポートしている場合)、
    まずNISネットグループとして解釈され、NISネットグループデータベースに名前が存在しない
    時は、UNIXのグループとして解釈される。</para>

    <para>
    「+」で始まる名前は、NSS getgrnam() インタフェース経由でUNIXのグループ名として
    のみ解釈される。「 &amp; 」で始まる名前は、NISネットグループの名前としてのみ解釈される
    (この場合システムでNISが稼働している必要がある)。
    名前の前に付けられた「+」および「 &amp; 」文字は、検索の順番を示す意味で使うこともできる。
    例えば、<parameter moreinfo="none">+&amp;group</parameter>は、
    UNIXグループを検索してから、NISネットグループを検索するという意味になり、
    <parameter moreinfo="none">&amp;+group</parameter>は、
    NIS ネットグループを検索してから UNIXグループを検索するという意味になる(これは「@」と同等になる)。
    </para>

    <para><parameter moreinfo="none">%S</parameter>は、
    現在のサービス名に置換される。これは [homes] セクションで有効である。</para>
</description>

<related>valid users</related>

<value type="default"><comment>invalid users は空である</comment></value>
<value type="example">root fred admin @wheel</value>
</samba:parameter>
