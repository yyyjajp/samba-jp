<samba:parameter name="username map script"
                 context="G"
		 type="string"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>
    このパラメーターは <smbconfoption name="username map"/> パラメーターと排他的な関係にある。
    このパラメーターは、（認証要求で渡されたユーザー名を示す）コマンドライン引数を1つだけとり、
    標準出力に（マッピングされたアカウント名を示す）文字列を1行返却する
    外部プログラム、もしくはスクリプトを指定する。    
    これにより、ユーザー名のマッピング表を LDAP や NIS
    などのディレクトリサービス上に格納することが可能となる。
    </para>
</description>
<value type="default"/>
<value type="example">/etc/samba/scripts/mapusers.sh</value>
</samba:parameter>
