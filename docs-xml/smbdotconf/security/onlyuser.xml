<samba:parameter name="only user"
					type="boolean"
                 context="S"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>

  <para>このパラメーターは使用されていない。</para>

  <para>しかしながら、<smbconfoption name="username"/>といっしょの時にのみ
  動作する。特定のユーザーに対してサービスを制限する、提供されている手法は、
  <smbconfoption name="valid users"/>パラメーターである。</para>

</description>

<related>user</related>

<value type="default">no</value>
</samba:parameter>
