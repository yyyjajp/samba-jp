<samba:parameter name="client signing"
                 context="G"
				 type="enum"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>このパラメーターは、SMB署名を使うために、クライアントに対して有効にするか、もしくは強制するかを設定する。
    設定可能な値は、<emphasis>auto</emphasis>、<emphasis>mandatory</emphasis>、<emphasis>disabled</emphasis> である。 
    </para>

    <para>auto または既定値に設定した場合、SMB 署名が有効になるが、
    Active Directory ドメインコントローラに対して強制される、winbindd をのぞき、強制はしない。</para>
    
    <para>mandatory に設定すると、SMB 署名が必須となる。disable に設定すると、SMB 署名は有効にならない。</para>
</description>
<value type="default">default</value>
</samba:parameter>
