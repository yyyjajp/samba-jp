<samba:parameter name="samba kcc command"
                 context="G"
                 type="string"
                 advanced="1" 
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このオプションは、Samba KCCコマンドへのパスを指定する。
	このスクリプトは複製トポロジを複製するために使われる。
	</para>

	<para>テスト目的か、<filename>samba_kcc</filename> が
	デフォルト以外のパスにインストールされた場合を除き、このオプションを
	変更する必要はない。
	</para>
</description>

<value type="default">$prefix/sbin/samba_kcc</value>
<value type="example">/usr/local/bin/kcc</value>
</samba:parameter>
