<samba:parameter name="send spnego principal"
                 context="G"
				 type="boolean"
                 advanced="1" developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>このパラメータは、<citerefentry><refentrytitle>smbd</refentrytitle>
    <manvolnum>8</manvolnum></citerefentry>が、SPNEGOの交換中において時々
    与えられたサーバから供給されたプリンシパルを送るかどうかを決める。</para>
    
    <para>This parameter determines whether or not
    <citerefentry><refentrytitle>smbd</refentrytitle>
    <manvolnum>8</manvolnum></citerefentry> will send the
    server-supplied principal sometimes given in the SPNEGO
    exchange.</para>

    <para>もしも有効にした場合、Sambaはたとえサービスプリンシパル名として
    使用するKDEにIPアドレスか名前が登録されていない場合のみだと知った時でも
    クライアントが、Kerberosで接続することを手助けする事を試みる事が出来る。
    サーバへ接続するためにKerberosを使うことを試みることが出来る。Kerberos
    は名前に依存するので、この状況においては、通常機能しない。</para>

    <para>If enabled, Samba can attempt to help clients to use
    Kerberos to contact it, even when known only by IP address or a
    name not registered with our KDC as a service principal name.
    Kerberos relies on names, so ordinarily cannot function in this
    situation. </para>

    <para>もしも無効にすると、Sambaは、'rfc4178 hint'として
    not_defined_in_RFC4178@please_ignoreを送り、この領域において、
    更新されたRFCとWindows 2008の動作に従う。</para>

    <para>If disabled, Samba will send the string
    not_defined_in_RFC4178@please_ignore as the 'rfc4178 hint',
    following the updated RFC and Windows 2008 behaviour in this area.
    </para>

    <para>Windows XP2と以降のバージョンはすでにすべての状況に応じて
    この値を無視することに注意。</para>
</description>
<value type="default">no</value>
</samba:parameter>
