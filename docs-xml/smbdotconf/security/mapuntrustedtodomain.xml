<samba:parameter name="map untrusted to domain"
                 context="G"
		 type="boolean"
		 advanced="1"
		 developer="1"
		 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    クライアントが BOGUS\user のような信頼されないドメイン名を使って
    smbd に接続する場合、 smbd は BOGUS ドメインを自身の SAM 名に
    置き換えてからそのユーザーを認証する。
    smbd が PDC として動作している場合、置き換えられた名前は DOMAIN\user となる。
    smbd がドメインのメンバーサーバーもしくはスタンドアロンサーバーとして
    動作している場合、置き換えられた名前は WORKSTATION\user となる。
  </para>

  <para>
    Samba 3.4.0 より前のバージョンでは、 smbd がドメインのメンバーサーバーとして
    動作している場合、 BOGUS の部分は smbd がメンバーとして所属
    しているドメイン名に置き換えられていた。これにより、
    認証は DOMAIN\user という認証情報を使って DC に委任されていた。
  </para>

  <para>
    このパラメーターが <constant>yes</constant> に設定されている場合、
    smbd は信頼されないドメイン名を所属しているドメイン名に置き換えるという
    従来の動作を行う。 smbd がドメインのメンバーサーバーとして動作していない場合、
    このパラメーターは意味を持たない。
  </para>

</description>

<value type="default">no</value>
</samba:parameter>
