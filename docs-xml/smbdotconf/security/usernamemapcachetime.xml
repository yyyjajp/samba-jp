<samba:parameter name="username map cache time"
                 context="G"
                 advanced="1" developer="0"
				 type="integer"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    Samba の <smbconfoption name="username map"/> や
    <smbconfoption name="username map script"/> によるユーザー名の
    マッピングは、比較的負荷が高い機能である。
    ユーザーのログオン時に、マッピング処理は何度も行われる。
    特に <smbconfoption name="username map script"/> が外部の
    データベースに対するクエリを行うような場合は、ログオン処理の遅延を
    引き起こすこともある。
  </para>

  <para>
    <smbconfoption name="username map cache time"/> は、
    マッピングのキャッシュを制御するパラメーターであり、
    username map file や username map script によるマッピング結果を
    キャッシュする期間を秒単位で指定する。
    デフォルトは 0 で、この場合キャッシュは行われない。
  </para>
</description>

<value type="default">0</value>
<value type="example">60</value>
</samba:parameter>
