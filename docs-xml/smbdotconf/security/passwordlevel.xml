<samba:parameter name="password level"
                 context="G"
				 type="integer"
                 advanced="1" developer="1"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
  <para>
    クライアントとサーバーの組合せによっては、
    大文字と小文字が混在したパスワードの扱いに問題が発生することがある。
    問題が発生するクライアントとしては、 LANMAN1 プロトコルを使用しているとパスワードを強制的に大文字にするが、
    COREPLUS を使用しているときはこの変換を行なわない Windows for Workgroups が挙げられる。
    Windows 95/98 系列のオペレーティングシステムにも問題がある。
    これらのクライアントは、プロトコルのネゴシエーションの結果
    NT LM 0.12が使われることになった場合でも、平文のパスワードを大文字化してしまう。
    </para>

  <para>
    このパラメーター (廃止予定) は、
    パスワード中に含まれる大文字の最大数を設定する。
  </para>

    <para> 例えば、与えられたパスワードが「FRED」であったとする。
    <parameter moreinfo="none">password level</parameter> が 1 に設定されていると、
    「FRED」で失敗したときに以下の組み合わせによる再試行が行なわれる:</para>

    <para>&quot;Fred&quot;, &quot;fred&quot;, &quot;fRed&quot;, &quot;frEd&quot;,&quot;freD&quot;</para>

    <para><parameter>password level</parameter> が 2 に設定されていると、
    以下の組合せによる試行も行なわれる: </para>

    <para>&quot;FRed&quot;, &quot;FrEd&quot;, &quot;FreD&quot;, &quot;fREd&quot;, &quot;fReD&quot;, &quot;frED&quot;, ……</para>

    <para>以降も同様である。</para>

    <para> このパラメーターを大きくするほど、
    大文字と小文字の混在したパスワードが大文字あるいは小文字のみのパスワードと一致するようになる。
    しかし、このパラメーターの利用はセキュリティの低下につながることと、
    新規のコネクションを処理するための時間が増大することを意識しておく必要がある。
    </para>

    <para> 値を 0 にすると、入力された通りのパスワードと、
    それらを全て小文字にした場合の 2 回の試行のみを行なう。</para>

<para>このパラメーターは、平文のパスワードを使用する場合にのみ使用される。暗号化パスワードが使用されている場合 (Samba 3.0.0 以降のデフォルト) にはまったく使用されない。つまり、このパラメーターは 
    <smbconfoption name="encrypt passwords">no</smbconfoption>
    という設定の場合にのみ使用されるということである。</para>
</description>

<value type="default">0</value>
<value type="example">4</value>
</samba:parameter>
