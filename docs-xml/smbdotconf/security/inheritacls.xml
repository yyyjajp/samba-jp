<samba:parameter name="inherit acls"
                 context="S"
				 type="boolean"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
    <para>このパラメータは、
    親ディレクトリにデフォルト ACL が設定されており、
    サブディレクトリの作成時に、その ACL の設定を引き継がせたい場合に用いられる。
    デフォルトでは ディレクトリ作成時に設定されるモードが用いられる。
    このオプションを有効にすると、モードが 0777 となり、
    ディレクトリのデフォルト ACL を確実に継承できるようになる。
    </para>
</description>

<value type="default">no</value>
</samba:parameter>
