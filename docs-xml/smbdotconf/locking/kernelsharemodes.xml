<samba:parameter name="kernel share modes"
		 type="boolean"
		 context="S"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>
        このパラメーターは、SMB 共有モードを UNIX の flock に変換するか
        どうかを制御する。
	</para>

	<para>
        このパラメーターは、SMB 共有モードに対応して flock を用いたアクセス
        制御を行うことで、ローカルの UNIX プロセスと NFS による操作との
        間での、最低限の相互運用性を提供する。
        一般的に、このパラメーターは有効のままにしておくことが望ましい。
	</para>

	<para>
        共有内で SMB2 の永続的ファイルハンドルを使うためには、このパラメーターを
        無効にする必要があることに注意すること。
	</para>

	<para>
	このパラメーターの既定値は<constant>yes</constant>だが、動作に必要な
        カーネルレベルの flock をサポートしていないシステム上では、自動的に
        無効化される。
	</para>
</description>

<value type="default">yes</value>
</samba:parameter>
