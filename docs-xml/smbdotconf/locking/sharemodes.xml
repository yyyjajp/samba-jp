<samba:parameter name="share modes"
	type="boolean"
                 context="S"
                 xmlns:samba="http://www.samba.org/samba/DTD/samba-doc">
<description>
	<para>このパラメータは、ファイルオープン中の <parameter moreinfo="none">share modes</parameter> の制御を有効にするかどうかを設定する。
	このモードは、ファイルへの排他的な読み出し又は書き込みアクセスを得るために、クライアントによって利用されるものである。
	</para>
	<para>
	これらのモードは、UNIX では提供されていないため、
	共有メモリ、共有メモリをサポートしていない場合はロックファイル(共有メモリはほとんどすべてのUNIXでサポートされている)で疑似的に実現されている。
	</para>

	<para>
	このオプションで有効化される共有モードは
	<constant>DENY_DOS</constant>、<constant>DENY_ALL</constant>,
	<constant>DENY_READ</constant>、<constant>DENY_WRITE</constant>,
	<constant>DENY_NONE</constant>、<constant>DENY_FCB</constant>
	である。</para>

	<para>このオプションは共有機能の互換性を保つため、
	デフォルトで有効になっている。</para>

	<para>このパラメータは、
	<emphasis>絶対に</emphasis> off にしてはならない。
	off にした場合、多くの Windows アプリケーションが誤動作するであろう。
	</para>
</description>
<value type="default">yes</value>
</samba:parameter>
